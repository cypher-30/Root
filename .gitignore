# Local machine config — never commit, points at your own SDK path
local.properties

# Local session handoff notes for solo/AI-assisted work — not teammate documentation.
# See docs/DEVELOPMENT.md for the durable, shared onboarding guide.
HANDOFF.md
claude.md
CLAUDE.md

# Build outputs
*.iml
.gradle/
build/
/captures/
.externalNativeBuild/
.cxx/
out/

# IDE
.idea/
*.hprof
.DS_Store

# Signing (if you ever add a real release keystore, never commit it)
*.jks
*.keystore
