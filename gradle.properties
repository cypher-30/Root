org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
# AGP 8.6.1 has only been tested up to compileSdk 35; app/build.gradle.kts targets 36
# (the newest platform installed locally) and it builds clean regardless — this just
# silences the advisory warning rather than fixing a real problem.
android.suppressUnsupportedCompileSdk=36
